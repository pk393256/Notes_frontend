import {TOGGLE} from './action';

let initialState = {
    title:'',
    status:false,
    
}
export function reducer(state=initialState,action){
    switch(action.type){
        case TOGGLE:
            return {...store,} 

    }
}